(1.5+3.5i) * (2-4i) # 複素数の計算 (iの前に数字がある場合のみ虚数とみなす)
1i * 1i # 虚数単位は i ではなく 1i 
1.38e10 * 3.68e-37 / 0.34e-5 # 指数表記の計算
-log(0) # 無限大 (非常に大きな値)
3 * log(0) # 数として扱える (計算はできる)
sqrt(-1) # Not a Number (非数)
sqrt(-1) + 1 # 数として扱えないので計算はできない
log(0) / log(0) # これも Not a Number (非数)
